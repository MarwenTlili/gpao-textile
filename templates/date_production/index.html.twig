{% extends 'base.html.twig' %}

{% block title %}DateProduction index{% endblock %}

{% block body %}
    <h1>DateProduction index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateDuJour</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for date_production in date_productions %}
            <tr>
                <td>{{ date_production.id }}</td>
                <td>{{ date_production.dateDuJour ? date_production.dateDuJour|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('date_production_show', {'id': date_production.id}) }}">show</a>
                    <a href="{{ path('date_production_edit', {'id': date_production.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('date_production_new') }}">Create new</a>
{% endblock %}
