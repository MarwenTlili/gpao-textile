<form method="post" action="{{ path('date_production_delete', {'id': date_production.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ date_production.id) }}">
    <button class="btn">Delete</button>
</form>
