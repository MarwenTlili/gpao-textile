{% extends 'layout.html.twig' %}

{% block title %}New EmployePresence{% endblock %}

{% block content %}
    <div class="container">
        <h1>Create new EmployePresence</h1>

        {{ include('employe_presence/_form.html.twig') }}

        {# <a href="{{ path('employe_presence_index') }}">back to list</a> #}
    </div>
{% endblock %}
