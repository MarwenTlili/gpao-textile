{% extends 'layout.html.twig' %}

{% block title %}EmployePresence{% endblock %}

{% block content %}
    <div class="container">
        <h1>EmployePresence</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ employe_presence.id }}</td>
                </tr>
                <tr>
                    <th>HeureDebut</th>
                    <td>{{ employe_presence.heureDebut ? employe_presence.heureDebut|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>HeureFin</th>
                    <td>{{ employe_presence.heureFin ? employe_presence.heureFin|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
            </tbody>
        </table>

        {# <a href="{{ path('employe_presence_index') }}">back to list</a> #}

        <a href="{{ path('employe_presence_edit', {'id': employe_presence.id}) }}">edit</a>

        {# {{ include('employe_presence/_delete_form.html.twig') }} #}
    </div>
{% endblock %}
