{% extends 'base.html.twig' %}

{% block title %}Presence{% endblock %}

{% block body %}
    <h1>Presence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presence.id }}</td>
            </tr>
            <tr>
                <th>DateJour</th>
                <td>{{ presence.dateJour ? presence.dateJour|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('presence_index') }}">back to list</a>

    <a href="{{ path('presence_edit', {'id': presence.id}) }}">edit</a>

    {{ include('presence/_delete_form.html.twig') }}
{% endblock %}
