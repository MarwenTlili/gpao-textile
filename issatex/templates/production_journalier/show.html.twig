{% extends 'base.html.twig' %}

{% block title %}ProductionJournalier{% endblock %}

{% block body %}
    <h1>ProductionJournalier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ production_journalier.id }}</td>
            </tr>
            <tr>
                <th>DateDuJour</th>
                <td>{{ production_journalier.dateDuJour ? production_journalier.dateDuJour|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('production_journalier_index') }}">back to list</a>

    <a href="{{ path('production_journalier_edit', {'id': production_journalier.id}) }}">edit</a>

    {{ include('production_journalier/_delete_form.html.twig') }}
{% endblock %}
