<form method="post" action="{{ path('production_journalier_delete', {'id': production_journalier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ production_journalier.id) }}">
    <button class="btn">Delete</button>
</form>
