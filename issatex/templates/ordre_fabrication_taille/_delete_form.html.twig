<form method="post" action="{{ path('ordre_fabrication_taille_delete', {'id': ordre_fabrication_taille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordre_fabrication_taille.id) }}">
    <button class="btn">Delete</button>
</form>
